-- Run as SYSTEM or another DBA user first:
-- CREATE USER auth_app IDENTIFIED BY auth_app;
-- GRANT CREATE SESSION, CREATE TABLE, CREATE SEQUENCE TO auth_app;
-- ALTER USER auth_app QUOTA UNLIMITED ON USERS;

-- Then connect as auth_app and run:
-- ROLE is one of STUDENT, TEAM_LEADER, MENTOR, ADMIN (see com.auth.entity.Role).
CREATE TABLE APP_USERS (
    USER_ID   NUMBER(19) PRIMARY KEY,
    NAME      VARCHAR2(100 CHAR) NOT NULL,
    EMAIL     VARCHAR2(150 CHAR) NOT NULL UNIQUE,
    PASSWORD  VARCHAR2(255 CHAR) NOT NULL,
    ROLE      VARCHAR2(20 CHAR) DEFAULT 'STUDENT' NOT NULL
);

CREATE SEQUENCE APP_USER_SEQ
    START WITH 1
    INCREMENT BY 1
    NOCACHE
    NOCYCLE;
