server.port=${PORT:${SERVER_PORT:8081}}

# --- MySQL datasource settings ---
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://bszu9qlblgoehnoomo4n-mysql.services.clever-cloud.com:3306/bszu9qlblgoehnoomo4n?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:ulszxy8wmqxeffij}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:mBuXpwhPH9B9kxiyIads}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT settings ---
app.jwt.secret=${JWT_SECRET:ProjectPilotYourAIAssistedGuideToBuildABetterProject}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
