server.port=8081

# --- MySQL datasource settings ---
spring.datasource.url=jdbc:mysql://localhost:3306/projectpilot?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=mithuS@06
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT settings ---
# IMPORTANT: this secret must be IDENTICAL to the one in project-service's
# application.properties. Both services use the same symmetric key.
app.jwt.secret=ProjectPilotYourAIAssistedGuideToBuildABetterProject
app.jwt.expiration-ms=3600000
